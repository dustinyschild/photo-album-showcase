import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";
import PhotoCard from "../../components/PhotoCard";

describe("PhotoCard", () => {
  it.todo("should display photo thumbnail");
  it.todo("should display the photo title");
});
